syntax = "proto3";

package tts;

service TTSService {
  rpc StreamTTS(stream Token) returns (stream AudioChunk);
}

message Token {
  string text = 1;
}

message AudioChunk {
  bytes pcm = 1;       // float32 PCM
  int64 timestamp = 2; // ms
}
